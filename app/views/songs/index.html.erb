<h1>Your Songs</h1>

<% @songs.each do |song| %>
  <div>
    <strong><%= song.title %></strong> by <%= song.artist %><br/>
    <%= link_to 'Play', rails_blob_path(song.audio_file), target: '_blank' if song.audio_file.attached? %>
  </div>
<% end %>

<%= link_to 'Back to Home', root_path %>
